import {profileAPI} from "../API/api";


let init = {
    profile: null
}
const settingsReducer = (state = init, action) => {
    switch (action.type) {
        default:
            return state;

    }
};



export default settingsReducer;
